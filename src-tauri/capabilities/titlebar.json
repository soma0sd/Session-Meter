{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "titlebar",
  "description": "Frameless custom title-bar controls for the settings and stats windows.",
  "windows": ["settings", "stats"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-minimize",
    "core:window:allow-unminimize"
  ]
}
