{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Widget-only: allow the frameless widget to be dragged from its own webview.",
  "windows": ["widget"],
  "permissions": ["core:window:allow-start-dragging", "core:window:allow-set-size"]
}
