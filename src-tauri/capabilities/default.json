{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for SessionMeter's local windows (settings, widget, stats, menu).",
  "windows": ["settings", "widget", "stats", "news", "menu"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
