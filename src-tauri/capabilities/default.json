{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for SessionMeter's local windows (settings, widget, stats, news, menu, style).",
  "windows": ["settings", "widget", "widget-*", "stats", "news", "menu", "style"],
  "permissions": ["core:default", "core:window:allow-hide"]
}
